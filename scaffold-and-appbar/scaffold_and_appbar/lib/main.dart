import 'package:flutter/material.dart';

void main() => runApp(
  MaterialApp(
    home:
      Scaffold(
        appBar: AppBar(
          title: Text("Sample AppBar and Scaffold"),
          backgroundColor: Colors.pink,
        )
      )
  )
);